describe('<TaskListView/>', () => {
  beforeAll(async () => {
    // await page.goto('http://localhost:8080/');
  });

  it.skip('shows "Nothing found" message if there are no tasks returned from server', async () => {
  });

  it.skip('shows equal number of entries as those returned from server', async () => {
  });

  it.skip('shows "New task has been successfully created" when form is submited', async () => {
  });

  it.skip('if tasks-view is directly opened then it doesn\'t show message "New task has been successfully created"', async () => {
  });
});
